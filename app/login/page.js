'use client'
import {useState} from 'react';import {useRouter} from 'next/navigation';import Image from 'next/image';import Link from 'next/link';import {supabase} from '../../lib/supabase'
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');const r=useRouter();async function go(e){e.preventDefault();const s=supabase();if(!s){setMsg('Supabase is not configured. Add the two environment variables in Vercel, then redeploy.');return}const {error}=await s.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else r.push('/dashboard')}return <div className="auth"><Image src="/logo.png" alt="iTrainSpeed" width={280} height={190} style={{objectFit:'contain'}}/><div className="card"><h1>Welcome back</h1><p>Sign in to manage training and performance.</p><form onSubmit={go}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button>Sign in</button></form>{msg&&<p className="error">{msg}</p>}<p>New parent? <Link href="/signup">Create an account</Link></p></div></div>}
