'use client'
import {useState} from 'react';import Link from 'next/link';import {supabase} from '../../lib/supabase'
export default function Signup(){const [f,setF]=useState({name:'',email:'',password:''}),[msg,setMsg]=useState('');async function go(e){e.preventDefault();const s=supabase();if(!s){setMsg('Supabase is not configured.');return}const {error}=await s.auth.signUp({email:f.email,password:f.password,options:{data:{full_name:f.name}}});setMsg(error?error.message:'Account created. Check your email if confirmation is enabled, then sign in.')}return <div className="auth"><div className="card"><h1>Create parent account</h1><form onSubmit={go}><label>Name<input value={f.name} onChange={e=>setF({...f,name:e.target.value})} required/></label><label>Email<input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} required/></label><label>Password<input type="password" minLength="6" value={f.password} onChange={e=>setF({...f,password:e.target.value})} required/></label><button>Create account</button></form>{msg&&<p>{msg}</p>}<Link href="/login">Back to sign in</Link></div></div>}
